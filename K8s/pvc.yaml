apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: duniversity-ns
spec:
  accessModes:
    - ReadWriteMany   
  storageClassName: nfs-storage-v2
  resources:
    requests:
      storage: 5Gi