apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-storage
provisioner: nfs.csi.k8s.io
paramaters:
  server: 10.0.2.222
  share: /mnt/data/duniversity
reclaimPolicy: Delete
volumeBindingMode: Immediate
allowVolumeExpansion: true